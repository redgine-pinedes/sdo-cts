# Example environment for SDO-CTS
# Copy to .env and fill values. Do NOT commit the .env file.

# Google OAuth
# Placeholders only; do not put real secrets in example files
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# Adjust for your local dev base URL if needed
GOOGLE_REDIRECT_URI=http://localhost/SDO-cts/admin/auth/google-callback.php
